@model IEnumerable<AZUL.COM.Models.SharedPage>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HotelName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FooterHeader)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FooterParagraph)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FooterCopyRight)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HotelName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FooterHeader)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FooterParagraph)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FooterCopyRight)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
